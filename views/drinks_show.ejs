<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= drink.name %></title>
    <style type="text/css">
        body{
            color: blanchedalmond;
            background-color: steelblue;
        }
    </style>
</head>
<body>
    <h1>At foo bar</h1>
    <ul>

        <% let capitalizedDrink = drink.name.slice(0,1).toUpperCase() + drink.name.slice(1); %>

        <h2>Name: <%= capitalizedDrink %> </h2>

        <h3>Price: <%= drink.price %> </h3>

        <img src=<%= drink.image.replace('.jpg', '.png') %>>

        </br>

        <a href="/drinks">Return Home</a>

    </ul>
</body>
</html>